@cmd_header pmuxtree@
<h1>pmuxtree - transform $pmux cells to trees of $mux cells</h1>
<pre>
    pmuxtree [options] [selection]

This pass transforms $pmux cells to a trees of $mux cells.

</pre>
@footer@
