@cmd_header ls@
<h1>ls - list modules or objects in modules</h1>
<pre>
    ls

When no active module is selected, this prints a list of all module.

When an active module is selected, this prints a list of objects in the module.

</pre>
@footer@
