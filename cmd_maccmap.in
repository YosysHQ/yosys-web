@cmd_header maccmap@
<h1>maccmap - mapping macc cells</h1>
<pre>
    maccmap [-unmap] [selection]

This pass maps $macc cells to yosys gate primitives. When the -unmap option is
used then the $macc cell is mapped to $and, $sub, etc. cells instead.

</pre>
@footer@
