@cmd_header proc_mux@
<h1>proc_mux - convert decision trees to multiplexers</h1>
<pre>
    proc_mux [selection]

This pass converts the decision trees in processes (originating from if-else
and case statements) to trees of multiplexer cells.

</pre>
@footer@
