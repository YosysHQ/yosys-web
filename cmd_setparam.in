@cmd_header setparam@
<h1>setparam - set/unset parameters on objects</h1>
<pre>
    setparam [ -set name value | -unset name ]... [selection]

Set/unset the given parameters on the selected cells. String values must be
passed in double quotes (").

</pre>
@footer@
